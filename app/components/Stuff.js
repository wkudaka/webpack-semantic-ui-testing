var React = require('react');


var Home = React.createClass({
  render: function() {
      return (
        <div>
          <h2>Stuff!</h2>
          <p>stuff page....</p>
        </div>
      );
    }
});

module.exports = Home;
